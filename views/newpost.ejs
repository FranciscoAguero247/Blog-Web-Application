<%- include("partials/header.ejs") %>
    <video autoplay muted loop id="background-video">
      <source src="/public/images/3 color Neon frame Video Background - lyric video Frame for Edits - Background video effects.mp4" type="video/mp4">
    </video>
    <% if (locals.post) { %>
      <p>
       Your post is: <%= post %>
      </p>
      <%}%>
    <div class="position-absolute top-50 start-50 translate-middle">
    <form action="/submit" method="POST">
      <input type="text" name="blogPost" placeholder="Type in a post" />
      <div class="dropdown">
        <button onclick="show_hide()" class="dropbtn" class="button-19" class="mobile" role="button"><i>Post</i></button>
        <select name="Blogs" value="blogs" class="dropdown-content">
          <option name="Car">Car</a>
          <option name="Star_Wars">Star Wars</a>
          <option name="Batman">Batman</a>
        </select>
      </div>
    </form>
  </div>
<%- include("partials/footer.ejs") %>